<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.mindease.mapper.ChatMessageMapper">
    
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO chat_message (session_id, user_id, message_role, content, create_time, update_time)
        VALUES (#{sessionId}, #{userId}, #{messageRole}, #{content}, #{createTime}, #{updateTime})
    </insert>
    
    <select id="selectBySessionId" resultType="com.mindease.pojo.entity.ChatMessage">
        SELECT id, session_id, user_id, message_role, content, create_time, update_time
        FROM chat_message
        WHERE session_id = #{sessionId}
        ORDER BY create_time ASC
        LIMIT #{limit}
    </select>
    
    <select id="selectBySessionIdAndUserId" resultType="com.mindease.pojo.entity.ChatMessage">
        SELECT id, session_id, user_id, message_role, content, create_time, update_time
        FROM chat_message
        WHERE session_id = #{sessionId} AND user_id = #{userId}
        ORDER BY create_time ASC
        LIMIT #{limit}
    </select>
    
    <delete id="deleteBySessionId">
        DELETE FROM chat_message
        WHERE session_id = #{sessionId}
    </delete>
    
</mapper>